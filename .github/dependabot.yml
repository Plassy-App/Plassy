version: 2
updates:
  # Umbrella root only. Submodule dirs (plassy-app, plassy-backend, etc.) are
  # separate private repos — Dependabot cannot clone them from this repo.
  - package-ecosystem: bun
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
