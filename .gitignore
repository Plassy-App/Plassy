# Local secrets (Sonar tokens, etc.)
.env
.env.local
.env.*.local
# PAT GitHub Packages — secrets only in gitignored root .envrc.local (committed .envrc is a stub)
/.envrc.local

# Sensitive material — clés privées, certificats, identifiants SSH
*.pem
*.key
*.p12
*.crt
*.cer
id_rsa
id_rsa.pub
/certs/
assets/

node_modules/
graphify-out/
.scannerwork/
# Cursor local state — keep rules and cloud env config versioned for the team
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/environment.json
.DS_Store
